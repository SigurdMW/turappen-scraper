{
	"name": "turappen-scraper",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"dev": "set NODE_ENV=dev && nodemon ./src/index.js",
		"start": "set NODE_ENV=production && node ./src/index.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"firebase-admin": "^11.4.1",
		"puppeteer": "^3.0.1"
	},
	"devDependencies": {
		"dotenv": "^8.2.0",
		"nodemon": "^2.0.3"
	}
}
